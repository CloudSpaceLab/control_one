# Control One UI (Placeholder)

This directory will host the React + TypeScript frontend for the Control One control plane.

## Planned structure
- `src/` – application source files (components, hooks, routes)
- `public/` – static assets and HTML template
- `package.json` – frontend tooling (Vite or CRA)

## Next steps
1. Scaffold the app (e.g., Vite + React + TypeScript).
2. Integrate OIDC auth guard and API client.
3. Add dashboard shell and navigation.
